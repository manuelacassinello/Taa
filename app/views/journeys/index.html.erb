<div id="journey-index" class="row">
  <div class="col-md-6 col-md-offset-1">

    <div class="journey-header">
      <h2> Trip Details 🌳 </h2>
    </div>

    <br>

    <div class="destination">
      <div id="origin">
        <h4> ⛳ From: <%=  @journeys %></h4>
      </div>
      <div id="final">
        <h4> 🔜 To: <%=  @journeys %></h4>
      </div>
    </div>

    <div>
      <% @journeys.each do |journey| %>
        <p> <strong><%= journey.transportation.upcase %></strong>: distance = <%= (journey.distance/1000).round(1) %> km | duration = <%= (journey.duration/60).round(0) %> minutes</p>
      <% end %>
    </div>

  </div>

    <div class="journey-panel">
      <h3 class="text-center">Current Status</h3>
    </div>
</div>
