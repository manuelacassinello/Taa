<div id="journey-index" class="row">
  <div class="col-md-6 col-md-offset-1">

      <div class="row">
      <h2> <%= link_to 'Where to Now 🌳', root_path %> </h2>
      </div>
      <br>

      <div>
        <% @journeys.each do |journey| %>
          <p> if you will be <strong><%= journey.transportation %></strong>, your journey will be <%= (journey.distance/1000).round(1) %> km long and last <%= (journey.duration/60).round(0) %> mins!!</p>
        <% end %>
      </div>


  </div>

    <div class="panel">
      <h3 class="text-center">Current Status</h3>
    </div>
</div>
